import { ArgTypes, Canvas, Markdown, Meta } from '@storybook/addon-docs';
import README from 'codemirror-markdown-image/README.md?raw';
import * as Stories from './markdown-image.stories';

<Meta of={Stories} />

<Markdown>{README}</Markdown>

## Plugin options
<ArgTypes />

## Demo
Use callback to control progress and async upload.

It is recommended to use custom cache to avoid repeated upload.

Call `command` anywhere to trigger upload, or dispatch `selectFileTriggerEffect`

<Canvas of={Stories.Basic} source={{ code: Stories.Basic.toString() }} />

### Use preview plugin only

it is available to use preview plugin only, just import `PreviewPlugin` and `previewStyle` optionally.

```typescript
new EditorView({
  extensions: [
    PreviewPlugin(),
    previewStyle
  ],
})
```

<Canvas of={Stories.PreviewOnly} source={{ code: Stories.PreviewOnly.toString() }} />

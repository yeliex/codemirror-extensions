{
  "name": "cmark-editor",
  "version": "0.0.1",
  "description": "markdown editor based on codemirror with image-uploader, tabbar and react extensions",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.0",
    "@types/node": "^18.11.7",
    "turbo": "^1.6.1",
    "typescript": "^4.8.4"
  }
}
